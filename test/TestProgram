float f;


start:
def main(integer t,pp | float s | out string size): void {
    var tp << 4;
    f << 1.5;

    for i << 1 to 10 loop {
        f <<  s + f ;
        tp << scegliOperazionep();
    }
    return ;
}

def scegliOperazione(integer it): void{
    var int << 12, f << "ciao";
    string i;
    i << "ciao";
    return;
}

def scegliOperazionep(): integer{
    var int << 12, f << "ciao";

    string i;
    return int;
}


real f;


start:
def main(integer t,pp | real s | out string size): void {
    var tp << 4;
    integer i << 0;
    f << 1.5;

    for i << 1 to 10 loop {
        f <<  s + f ;
        tp << 1 -2;
    }

    while(i < 20 or tp >-15) loop{
        i << i+1;
        tp << i - 2;
    }
    return ;
}



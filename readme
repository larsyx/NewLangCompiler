PROGETTO
    - https://gitlab.com/g4660/compilatori-a.a.-2022_23/newlang-semancgen-es5/moscato_es5.git

AVVIO
    - Per compilare il programma e produrre il file scritto in C sono stati creati diversi test da compilare ed eseguire da intellij;
    - Il file C generato è presente nella directory test_files/c_out;
    - Per compilare ed eseguire il programma scritto in C basta eseguire lo script CompileExec.sh <nomefile>.c;
    - Per eseguire un programma già compilato basta eseguire lo script Execute.sh <nomefile>.exe;
    - Lo script CompileAll compila tutti i test presenti nella cartella test_files/c_out;


SCELTE DI IMPLEMENTAZIONE
    - i numeri reali sono definiti da real;
    - Ogni funzione deve avere return;
    - La concatenazione di stringhe può avvenire solo tra due stringhe per la stampa usare la virgola;
        es. ("stampo numero:" , numero , "e" , secondo numero); -->!; dove numero e secondo numero sono interi;
    - Durante una chiamata a funzione i parametri out non hanno bisogno di @;
    - Esiste solo l'operatore DivOp, DivIntOp non implementato;
    - Tutte le variabili anche quelle globali per essere usate devono essere dichiarate precedentemente;
    - Non è possibile inserire nomi di variabili che sono key-word in c es. integer int o real float;
    - Firma main start: def <id>():void